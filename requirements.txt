# Companion code for Agentic AI in Production (Book 2)
# Python 3.11+ recommended

# LLM and agent SDKs
anthropic>=0.40.0

# Web frameworks and HTTP clients
fastapi>=0.110.0
starlette>=0.36.0
httpx>=0.27.0

# Identity, secrets, crypto
cryptography>=42.0.0
PyJWT>=2.8.0
hvac>=2.1.0                 # HashiCorp Vault client
boto3>=1.34.0               # AWS SDK (Secrets Manager, KMS, etc.)

# Persistence and caching
redis>=5.0.0
sqlalchemy>=2.0.0
cachetools>=5.3.0

# Validation and configuration
pydantic>=2.5.0
pydantic-settings>=2.1.0
PyYAML>=6.0.1

# Observability
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-exporter-otlp>=1.22.0
opentelemetry-instrumentation-fastapi>=0.43b0
prometheus-client>=0.19.0
structlog>=24.1.0

# Feature flags
launchdarkly-server-sdk>=9.4.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
hypothesis>=6.92.0
respx>=0.20.0
vcrpy>=6.0.0
